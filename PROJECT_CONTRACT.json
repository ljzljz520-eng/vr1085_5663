{
  "schema_version": 1,
  "metrics": {
    "production_loc": 2362,
    "production_files": 27,
    "production_packages": 8,
    "test_files": 9,
    "production_functions": 105,
    "decision_points": 89
  },
  "persistence": {
    "module": "modernc.org/sqlite",
    "entities": ["SessionRecord", "GatewayKey", "ParameterSet", "AuditEntry"],
    "reopen_test": "TestPersistenceSurvivesReopen"
  },
  "workflows": [
    {"steps": ["register gate public key", "validate parameter set", "wrap daily secret", "persist session and audit summary"], "test": "TestWorkflowNegotiation"},
    {"steps": ["load persisted session", "validate expiry", "unwrap ciphertext", "verify digest and return secret"], "test": "TestWorkflowRecovery"},
    {"steps": ["query active session", "transition state to closed", "persist lifecycle update", "confirm closed state"], "test": "TestWorkflowRetirement"},
    {"steps": ["open sqlite store", "write session entities", "close store", "reopen and query entities"], "test": "TestPersistenceSurvivesReopen"}
  ]
}
